# Waterschapsgrens

Waterschapsgrens als sf-object in RD-stelstel (CRS 28992) en WGS84 (CRS
4326).

## Usage

``` r
ws_grens_rd

ws_grens_wgs
```

## Format

An object of class `sf` (inherits from `tbl_df`, `tbl`, `data.frame`)
with 5 rows and 3 columns.

An object of class `sf` (inherits from `tbl_df`, `tbl`, `data.frame`)
with 5 rows and 3 columns.
